/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['assets.aceternity.com','encrypted-tbn1.gstatic.com'], // Add the external domain here
    },
   };
   
   export default nextConfig;